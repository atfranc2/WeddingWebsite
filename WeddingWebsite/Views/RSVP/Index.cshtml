@{
    ViewData["Title"] = "RSVP";
}


<form id="guestLookup">
    <div class="form-group">
        <label>Full Name</label>
        <div class="tt-container">
            <input id="fullName" required type="text" value="" class="form-control" />
        </div>
    </div>
    <button class="btn btn-primary">Find Your Invitation</button>
</form>

@*window.location.href = "@Url.Action("New", "Guest")";*@

@section scripts
{
    <script src="~/lib/typeahead.js/typeahead.bundle.js"></script>
    <script>

        function lookupGuestByName(fullName) {

        }

        $(document).ready(function () {
            var guests = new Bloodhound({
                datumTokenizer: Bloodhound.tokenizers.obj.whitespace('fullName'),
                queryTokenizer: Bloodhound.tokenizers.whitespace,
                remote: {
                    url: '/api/Guests?query=%QUERY',
                    wildcard: '%QUERY'
                }
            });
            var vm = {};

            $('#fullName').typeahead({
                minLength: 3,
                highlight: true
            }, {
                name: 'guests',
                display: 'fullName',
                source: guests
            }).on("typeahead:select", function (e, guest) {
                vm.Id = guest.id;
                vm.FirstName = guest.firstName;
                vm.MiddleName = guest.middleName;
                vm.LastName = guest.lastName;
                vm.FullName = guest.FullName;
                vm.PlusOneAllowed = guest.plusOneAllowed;
                vm.PartOfCouple = guest.partOfCouple;
            });

            $("#guestLookup").submit(function (e) {
                e.preventDefault();
                window.location.href = "@Url.Action("RSVPForm", "RSVP")/" + vm.Id;
            });

        });

    </script>
}


